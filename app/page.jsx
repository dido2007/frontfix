import TabCard from '@components/Marketplace/TabCard';

const Home = () => {
  return (
    <section className="w-full flex-center flex-col">
      <TabCard />
    </section>
  );
};

export default Home;
